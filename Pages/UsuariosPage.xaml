<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:Rba.Views"
             x:Class="Rba.Pages.UsuariosPage"
             Title="Usuários Cadastrados">
    
    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="15">

            <views:LogoView />

            <!-- Aviso visual -->
            <Label Text="Atenção! Excluir um usuário é uma ação irreversível. Clique no usuário que deseja excluir."
                   FontSize="20" HorizontalTextAlignment="Center"
                   HorizontalOptions="Center"
                   TextColor="{StaticResource Primary}" />

            <Button Text="Consultar" Clicked="OnConsultarClicked"
                    Background="{StaticResource PrimaryBrush}"
                    TextColor="{StaticResource White}" />

            <Entry x:Name="ConsultarEntry"
                   Placeholder="Digite o usuário que deseja buscar e clique em consultar"
                   TextChanged="OnConsultarTextChanged"
                   BackgroundColor="White"
                   Margin="0,10,0,0"
                   FontSize="12"
                   TextColor="Black"/>

            <Button Text="Voltar" Clicked="OnVoltarClicked"
                Background="{StaticResource PrimaryBrush}"
                TextColor="{StaticResource White}" />
            
            <!-- local do banco - exibir caminho do banco no app -->
            <!--
            <Button Text="Ver caminho do banco" Clicked="OnVerCaminhoClicked"
                    BackgroundColor="DarkGreen"
                    TextColor="White"
                    Margin="10" />
               -->


            <CollectionView x:Name="UsuariosListView"
                        SelectionMode="Single"
                        SelectionChanged="OnUsuarioSelecionado">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <!-- Corrigido: Removido BorderColor, pois não existe essa propriedade no Border do .NET MAUI -->
                        <Border Stroke="LightGray" StrokeThickness="1" Padding="10" Margin="5">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="{Binding ID}" FontAttributes="Bold"/>
                                <Label Text="{Binding NomeUsuario}" />
                                <Label Text="{Binding Email}" />
                                <Label Text="{Binding TipoUsuario}" />
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>